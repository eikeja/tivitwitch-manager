version: '3.8'

services:
  tivitwitch:
    build: .
    container_name: tivitwitch-manager
    restart: unless-stopped
    ports:
      - "8998:8000"
    environment:
      - HOST_URL=${HOST_URL}
    volumes:
      - tivitwitch_data:/data

volumes:
  tivitwitch_data: